replicaCount: 1

image:
  repository: ghcr.io/flanksource/mission-control-plugins/s3
  tag: ""
  pullPolicy: IfNotPresent

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

podAnnotations: {}
podLabels: {}

service:
  type: ClusterIP
  port: 9000

plugin:
  # Address Mission Control dials to reach this plugin's gRPC server (host:port).
  # Leave empty if Mission Control is configured to reach the plugin another way.
  address: ""

resources: {}

nodeSelector: {}
tolerations: []
affinity: {}

extraEnv: []
extraArgs: []
