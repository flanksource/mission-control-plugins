name: Build Plugins

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  build:
    name: Build ${{ matrix.plugin }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        plugin:
          - arthas
          - golang
          - inspektor-gadget
          - kubernetes-logs
          - postgres
          - sql-server

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Check plugin changes
        id: changes
        env:
          PLUGIN: ${{ matrix.plugin }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_REF: ${{ github.base_ref }}
          BEFORE: ${{ github.event.before }}
        run: |
          if [ "$EVENT_NAME" = "pull_request" ]; then
            git fetch --no-tags --depth=1 origin "$BASE_REF"
            changed_files=$(git diff --name-only "origin/$BASE_REF"...HEAD)
          elif [ -n "$BEFORE" ] && ! printf '%s' "$BEFORE" | grep -q '^0\+$'; then
            changed_files=$(git diff --name-only "$BEFORE"..HEAD)
          else
            changed_files=$(git diff --name-only HEAD~1..HEAD)
          fi

          if printf '%s\n' "$changed_files" | grep -Eq "^($PLUGIN/|Makefile|Taskfile.yml|\.github/workflows/build\.yaml)"; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup Go
        if: steps.changes.outputs.changed == 'true'
        uses: actions/setup-go@v6
        with:
          go-version-file: ${{ matrix.plugin }}/go.mod
          cache-dependency-path: ${{ matrix.plugin }}/go.sum

      - name: Setup Node
        if: steps.changes.outputs.changed == 'true'
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Setup pnpm
        if: steps.changes.outputs.changed == 'true'
        uses: pnpm/action-setup@v4
        with:
          version: 10.33.0
          run_install: false

      - name: Build UI
        if: steps.changes.outputs.changed == 'true'
        working-directory: ${{ matrix.plugin }}/ui-src
        env:
          PLUGIN_VERSION: ${{ github.sha }}
          PLUGIN_BUILD_DATE: ${{ github.event.repository.updated_at }}
        run: |
          pnpm install --frozen-lockfile
          pnpm run build

      - name: Regenerate UI checksum
        if: steps.changes.outputs.changed == 'true'
        working-directory: ${{ matrix.plugin }}
        run: go generate ./...

      - name: Build Go binary
        if: steps.changes.outputs.changed == 'true'
        working-directory: ${{ matrix.plugin }}
        run: go build .
